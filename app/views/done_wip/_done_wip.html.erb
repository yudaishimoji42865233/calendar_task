<% if task.status == "Done" %>
  <%= link_to 'WIP', wip_path(task), method: :post, remote: true, class: "btn btn-warning" %>
<% else %>
  <%= link_to 'Done', done_path(task), method: :post, remote: true, class: "btn btn-info" %>
<% end %>